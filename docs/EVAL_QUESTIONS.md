# EVAL_QUESTIONS.md — CareerFit AI 자기 평가 질문

## 이 질문들에 스스로 답할 수 있어야 최종 발표와 면접에 나설 수 있다.

## 프로젝트 전체
- 이 서비스가 해결하는 문제는 무엇인가? 취준생들이 자신의 역량과 채용 공고를 효율적으로 비교할 수 있음
- RAG를 사용하지 않으면 어떤 문제가 생기는가? 기존 학습된 지식으로만 답변(존재하지 않는 채용공고를 만드는 등..)
- 가장 어려웠던 구현은 무엇이고, 어떻게 해결했는가?

## 데이터 관련
- 왜 이 데이터를 선택했는가?
- 데이터의 어떤 부분이 신뢰할 수 없을 수 있는가?
- SQLite와 ChromaDB에 같은 데이터를 저장하는 이유는 무엇인가?
- 결측치와 중복을 어떻게 처리했는가?

## RAG 관련
- RAG 구조에서 ChromaDB는 어떤 역할을 하는가? 채용공고 자료를 찾아주는 역할
- n_results 값을 높이면 어떤 트레이드오프가 생기는가? 값이 너무 크면 관련성이 낮은 문서까지 포함 가능
- 검색 결과 품질을 어떻게 검증했는가?
- Hallucination이 무엇이며, RAG가 이를 어떻게 줄이는가? llm이 실제 근거없이 사실이 아닌 내용을 그럴듯하게 생성하는 현상, rag는 검색된 데이터로만 답변하도록 유도

## API 관련
- /analyze 엔드포인트가 POST를 사용하는 이유는 무엇인가? 단순 조회가 아니라 사용자가 입력한 데이터를 바탕으로 새로운 분석결과를 생성하기 때문에
- 응답 JSON에 sources를 포함시킨 이유는 무엇인가? 답변의 근거를 사용자에게 제공
- /health 엔드포인트가 왜 필요한가? 서버가 정상적으로 실행되고 있는지 확인하기 위해

## 보안 관련
- .env 파일을 GitHub에 올리면 어떤 일이 생기는가?
- .env와 .env.example의 차이는 무엇인가?

## Docker 관련
- Docker를 사용하는 이유는 무엇인가?
- Dockerfile에서 EXPOSE와 CMD의 역할은 무엇인가?